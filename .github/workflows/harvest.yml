name: Transform GeoNames data to RDF

on:
  schedule:
    - cron: '0 3 * * 0' # Every Sunday at 03:00 UTC
  workflow_dispatch:
    inputs:
      converter:
        description: 'Which of the two conversions to run: the shell scripts or the LDE port'
        type: choice
        options:
          - shell
          - lde
        default: shell

jobs:
  run:
    runs-on: ubuntu-latest
    # The scheduled run has no inputs, so it takes the shell scripts; a manual run can pick the LDE
    # port instead. Both write output/geonames.nt, and everything after that is shared.
    env:
      CONVERTER: ${{ inputs.converter || 'shell' }}
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-java@v6
        with:
          distribution: 'temurin'
          java-version: '21'

      - if: env.CONVERTER == 'shell'
        run: ./download.sh
      - if: env.CONVERTER == 'shell'
        run: ./map.sh

      - if: env.CONVERTER == 'lde'
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
      - if: env.CONVERTER == 'lde'
        run: npm ci
      - if: env.CONVERTER == 'lde'
        run: node lde/download.ts
      - if: env.CONVERTER == 'lde'
        run: node lde/map.ts

      - uses: actions/upload-artifact@v7
        with:
          name: geonames-nt
          path: output/geonames.nt
      # Publish two encodings of the same N-Triples file:
      # - geonames.nt.gz is the one to consume. Loaders read gzip natively (Jena infers it from
      #   the .gz extension), so they load it without an unzip step or staging ~14 GB of plain text.
      # - geonames.zip stays for existing consumers of that URL, which we must not break.
      # Both use DEFLATE level 9: it is ~10% smaller than zip's default level 6 (858 MB -> ~773 MB)
      # for ~3x the CPU, which costs a few minutes on a job that already runs 24 and only weekly.
      - run: |
          cd output
          gzip -9 -k geonames.nt
          zip -9 geonames.zip geonames.nt
      - uses: BetaHuhn/do-spaces-action@v2
        with:
          access_key: ${{ secrets.S3_ACCESS_KEY}}
          secret_key: ${{ secrets.S3_SECRET_KEY }}
          space_name: geonames
          space_region: ams3
          source: output/geonames.nt.gz
      - uses: BetaHuhn/do-spaces-action@v2
        with:
          access_key: ${{ secrets.S3_ACCESS_KEY}}
          secret_key: ${{ secrets.S3_SECRET_KEY }}
          space_name: geonames
          space_region: ams3
          source: output/geonames.zip
